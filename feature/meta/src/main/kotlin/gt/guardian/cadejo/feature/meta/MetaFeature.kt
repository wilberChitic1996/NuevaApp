package gt.guardian.cadejo.feature.meta

/**
 * Placeholder anchor for the meta-progression feature (shop, unlocks, signed
 * save). Real screens and view models arrive in Phase 3. Kept so the module is a
 * valid, compilable part of the graph from day one.
 */
internal object MetaFeature {
    const val ROUTE = "meta"
}
